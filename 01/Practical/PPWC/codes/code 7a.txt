#include<stdio.h>
#include<conio.h>
#include<string.h>
void demo(char*s,int start,int end)
{
 int i;
 for(i=start;i<=end;i++)
    printf("%c",s[i]);
}
int main()
{ printf("01-AlstonAlvares\n");
char str[100]="NAVI MUMBAI";
int s,e;
printf("Enter the Start Index:");
scanf("%d",&s);
printf("Enter the Last Index:");
scanf("%d",&e);
if(e>strlen(str) ||(s>strlen(str)))
printf("The indeex's starting or ending value is out of range ");
else 
demo(str,s,e);
return 0;
}