#include<stdio.h>
int main()
{ printf("01-AlstonAlvares.\n");
    int n; 
    for(;;)      /*ifinite loop*/
    {
  printf("enter any number :");
  scanf("%d",&n);
  if(n == 5)
goto ap;        /* use of goto statement*/
if (n% 2 == 0)
  continue;   /*use of continue statement*/
if (n% 3 == 0)
break;   /*use of break state*/
 printf("Inside loop");
}
ap:
printf("Outside loop");
return 0;
}